# The apify SDK does NOT provide a "playwright" extra (its only extra is
# "scrapy"), so Playwright must be declared explicitly. Installing
# apify[playwright] silently emits a warning and gives you no browser.
apify ~= 4.0
playwright ~= 1.50
httpx ~= 0.27
python-dateutil ~= 2.9
